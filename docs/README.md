# Docs

`docs/` stores auxiliary development material for maintaining this repository:
design iterations, review rounds, migration notes, and similar working artifacts.

These files are not part of the distributed skill/plugin runtime surface.
The packaged deliverables live under `skills/`, `plugins/dev-workflow-skills/`,
`.agents/plugins/marketplace.json`, and `.claude-plugin/`.
